name: esc_pos_bluetooth
description: The library allows to print receipts using an ESC/POS thermal Bluetooth printer.
version: 0.4.1-null-safety.0
homepage: https://github.com/andrey-ushakov/esc_pos_bluetooth
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.26.0
  esc_pos_utils:
    git:
      url: https://github.com/kahero-team/esc_pos_utils_ticket.git
  flutter_bluetooth_basic:
    git:
      url: https://github.com/kahero-team/flutter_bluetooth_basic.git
      ref: feature/refactoring-for-android-gradle-plugin-upgrade

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
